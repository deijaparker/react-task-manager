import React from "react";
import ReactDOM from "react-dom/client";
import AppFunction from "./Container/AppFunction"; // Correct import for your main component
import "./index.css"; // Importing the styles

// Render the AppFunction component
ReactDOM.createRoot(document.getElementById("root")).render(<AppFunction />);
